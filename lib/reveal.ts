// The flower and the message share one clock: each step reveals one petal and
// the slice of text that goes with it, so both finish at the same time.
export const REVEAL_STEPS = 144; // Fibonacci number, a nod to the golden ratio
export const REVEAL_INTERVAL_MS = 25;
